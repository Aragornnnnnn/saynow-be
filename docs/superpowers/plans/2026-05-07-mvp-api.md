# SayNow MVP API Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement the SayNow backend MVP schema, seed data, and client APIs described in `/Users/sangmin8817/Desktop/Obsidian/SayNow/백엔드 MVP - 테이블 구조 및 API 명세.md`.

**Architecture:** Keep a single Spring Boot application under `com.saynow`, split by feature package: `scenario`, `practice`, `feedback`, and `common`. Use Flyway for PostgreSQL schema and seed data, Spring Data JPA for persistence, MockMvc integration tests for API contracts, and a replaceable local AI client adapter so the MVP can run without a real AI server.

**Tech Stack:** Java 21, Spring Boot 4.0.6, Spring MVC, Spring Data JPA, Validation, Flyway, PostgreSQL, H2 for integration tests, JUnit 5, MockMvc.

---

## File Structure

- Modify: `build.gradle.kts`
  - Add H2 as a test runtime dependency for integration tests.
- Modify: `src/main/resources/db/migration/V1__init.sql`
  - Keep the initial marker migration unchanged.
- Create: `src/main/resources/db/migration/V2__mvp_schema_and_seed.sql`
  - Create MVP tables and seed 5 categories, 10 scenarios, and required slots.
- Create: `src/main/java/com/saynow/common/exception/*`
  - Central API exception and validation error handling.
- Create: `src/main/java/com/saynow/scenario/**`
  - Scenario/category entities, repositories, DTOs, service, and controller.
- Create: `src/main/java/com/saynow/practice/**`
  - Session, prompt, turn, metric, slot-value entities, repositories, DTOs, local AI adapter, service, and controller.
- Create: `src/main/java/com/saynow/feedback/**`
  - Feedback entities, repositories, DTOs, service, and controller.
- Create: `src/test/java/com/saynow/IntegrationTestSupport.java`
  - Shared Spring Boot and H2 test configuration.
- Create: `src/test/java/com/saynow/scenario/ScenarioApiIntegrationTest.java`
  - Validate category/scenario APIs and that internal slots are not exposed.
- Create: `src/test/java/com/saynow/practice/PracticeSessionApiIntegrationTest.java`
  - Validate start, mic metric, turn submit, exit, terminal conflict, and feedback flow.

## Tasks

### Task 1: Failing API Contract Tests

**Files:**
- Create: `src/test/java/com/saynow/IntegrationTestSupport.java`
- Create: `src/test/java/com/saynow/scenario/ScenarioApiIntegrationTest.java`
- Create: `src/test/java/com/saynow/practice/PracticeSessionApiIntegrationTest.java`
- Modify: `build.gradle.kts`

- [ ] Add H2 test runtime dependency:

```kotlin
testRuntimeOnly("com.h2database:h2")
```

- [ ] Add `IntegrationTestSupport` with `@SpringBootTest`, `@AutoConfigureMockMvc`, and H2 PostgreSQL-mode properties.
- [ ] Add scenario API tests for:
  - `GET /api/v1/categories`
  - `GET /api/v1/categories/cafe/scenarios`
  - `GET /api/v1/scenarios/cafe_iced_americano`
  - `GET /api/v1/categories/unknown/scenarios` returns `404`
- [ ] Add practice API tests for:
  - `POST /api/v1/sessions`
  - `PUT /api/v1/sessions/{sessionId}/metrics/micReady`
  - `POST /api/v1/sessions/{sessionId}/turns`
  - `GET /api/v1/sessions/{sessionId}`
  - `GET /api/v1/sessions/{sessionId}/feedback`
  - terminal session update returns `409`
  - exit then metric update returns `409`
- [ ] Run:

```bash
./gradlew test
```

Expected: tests fail because the schema, controllers, services, and DTOs do not exist yet.

### Task 2: MVP Schema And Seed Data

**Files:**
- Create: `src/main/resources/db/migration/V2__mvp_schema_and_seed.sql`

- [ ] Create all tables from the MVP spec using PostgreSQL-compatible types and constraints.
- [ ] Add indexes and unique constraints from the spec.
- [ ] Seed categories: `airport`, `hotel`, `cafe`, `restaurant`, `taxi`.
- [ ] Seed two scenarios per category.
- [ ] Seed required scenario slots for each scenario.
- [ ] Run:

```bash
./gradlew test --tests com.saynow.scenario.ScenarioApiIntegrationTest
```

Expected: tests still fail because Java API code is not implemented, but Flyway should migrate successfully.

### Task 3: Scenario Read APIs

**Files:**
- Create: `src/main/java/com/saynow/scenario/domain/*`
- Create: `src/main/java/com/saynow/scenario/repository/*`
- Create: `src/main/java/com/saynow/scenario/service/ScenarioService.java`
- Create: `src/main/java/com/saynow/scenario/controller/ScenarioController.java`
- Create: `src/main/java/com/saynow/scenario/controller/dto/*`
- Create: `src/main/java/com/saynow/common/exception/*`

- [ ] Implement category and scenario JPA entities.
- [ ] Implement repositories with active-only ordered queries.
- [ ] Implement `ScenarioService`.
- [ ] Implement `ScenarioController` paths from the spec.
- [ ] Add `ApiException` and `GlobalExceptionHandler` for `400`, `404`, and `409` JSON errors.
- [ ] Run:

```bash
./gradlew test --tests com.saynow.scenario.ScenarioApiIntegrationTest
```

Expected: scenario API tests pass.

### Task 4: Practice Session And Turn APIs

**Files:**
- Create: `src/main/java/com/saynow/practice/domain/*`
- Create: `src/main/java/com/saynow/practice/repository/*`
- Create: `src/main/java/com/saynow/practice/service/*`
- Create: `src/main/java/com/saynow/practice/controller/*`
- Create: `src/main/java/com/saynow/practice/controller/dto/*`

- [ ] Implement practice session, prompt, metric, turn, and slot-value entities.
- [ ] Implement repositories for lookup by public session ID and ordered prompts/turns.
- [ ] Implement session start:
  - create `practice_sessions`
  - create opening `session_prompts`
  - return `201`
- [ ] Implement mic-ready metric upsert.
- [ ] Implement turn submission:
  - reject non-`IN_PROGRESS` sessions with `409`
  - save `practice_turns`
  - call local AI adapter
  - validate and save known slots only
  - create follow-up prompt, success result prompt, or failure result prompt
  - never expose `filledSlots` or `missingSlots` in client responses
- [ ] Implement session status lookup and exit.
- [ ] Run:

```bash
./gradlew test --tests com.saynow.practice.PracticeSessionApiIntegrationTest
```

Expected: practice API tests pass except feedback lookup until Task 5 is complete.

### Task 5: Feedback Persistence And Lookup

**Files:**
- Create: `src/main/java/com/saynow/feedback/domain/*`
- Create: `src/main/java/com/saynow/feedback/repository/*`
- Create: `src/main/java/com/saynow/feedback/service/FeedbackService.java`
- Create: `src/main/java/com/saynow/feedback/controller/FeedbackController.java`
- Create: `src/main/java/com/saynow/feedback/controller/dto/*`
- Modify: `src/main/java/com/saynow/practice/service/PracticeSessionService.java`

- [ ] Implement feedback entities and repositories.
- [ ] Extend local AI adapter to generate deterministic MVP feedback for terminal success/failure sessions.
- [ ] Persist `session_feedbacks` and `turn_feedbacks` synchronously when a session ends by success/failure.
- [ ] Implement `GET /api/v1/sessions/{sessionId}/feedback`:
  - `400` for in-progress sessions
  - `202` for generating feedback
  - `404` for missing feedback
  - `200` for ready feedback
- [ ] Run:

```bash
./gradlew test --tests com.saynow.practice.PracticeSessionApiIntegrationTest
```

Expected: all practice and feedback tests pass.

### Task 6: Final Verification

**Files:**
- Modify only if tests reveal contract gaps.

- [ ] Run full tests:

```bash
./gradlew test
```

- [ ] Run build:

```bash
./gradlew build
```

- [ ] Review diff:

```bash
git diff --stat
git diff
```

- [ ] Commit work by completed unit:
  - `mvp-api docs: MVP API 구현 계획 작성`
  - `mvp-api feat: MVP 스키마와 시드 데이터 추가`
  - `mvp-api feat: 시나리오 조회 API 구현`
  - `mvp-api feat: 세션 진행 API 구현`
  - `mvp-api feat: 피드백 조회 API 구현`

